DROP TABLE if EXISTS IDs;

CREATE TABLE IDs(
  bsc VARCHAR(255),
  bts VARCHAR(255),
  bts_id VARCHAR(255),
  bts_name VARCHAR(255),
  bsc_of_trx VARCHAR(255),
  bts_of_trx VARCHAR(255),
  trx VARCHAR(255),
  trx_id VARCHAR(255)
);

